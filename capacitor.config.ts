import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'metric_converter-yonathan_sihotang-ionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
